<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--suppress ALL -->
<mapper namespace="cn.ddzu.shop.dao.PriceRangeMapperDao">
    <update id="drop">
        drop table if exists price_range
    </update>

    <update id="create">
        create table price_range
        (
            id   bigint auto_increment,
            name varchar(255),
            primary key (id)
        ) default charset = utf8
    </update>

    <update id="init">
        insert into price_range(name)
        values ('全部'),
               ('0-3元/m²/天'),
               ('3-4元/m²/天'),
               ('4-5元/m²/天'),
               ('5-7元/m²/天'),
               ('7-9元/m²/天'),
               ('9-12元/m²/天'),
               ('12元/m²/天以上')
    </update>

    <select id="select" resultType="cn.ddzu.shop.entity.PriceRange">
        select * from price_range order by id asc
    </select>

    <select id="selectByBuilding"
            resultType="cn.ddzu.shop.entity.AreaRange"
            parameterType="java.lang.Long">
        select * from price_range where id in
        (
            select price_range_id as id
            from office where building_id = #{building_id}
            group by price_range_id
        ) or id = 1 order by id asc
    </select>
</mapper>